## Release Notes

### 0.1.0 – Initial Version

- **Project setup**: Initial structure for `saucedemo-ui-tests-with-selenium` with page objects, locators, test data, and utilities.
- **Test framework**: Configured `pytest` with fixtures, base test class, and HTML reporting via `pytest-html`.
- **Browser automation**: Selenium WebDriver setup with Chrome in headless and UI modes, including configuration for CI/CD and Docker.
- **Logging and reporting**: Added dual logging (file logs + HTML reports) with detailed test execution traces.
- **Tooling**: Integrated `uv` for dependency management and `ruff` for linting/formatting, plus `Makefile` commands for common workflows.


